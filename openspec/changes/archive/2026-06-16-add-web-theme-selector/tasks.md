## 1. Settings page foundation

- [x] 1.1 Add a settings navigation entry, route, and page scaffold that fits the existing web workspace layout.
- [x] 1.2 Move the language selector from the sidebar header into a general preferences section on the settings page.

## 2. Theme preferences and shared styling

- [x] 2.1 Add client-side theme preference resolution and persistence for light, dark, and system modes through the settings page.
- [x] 2.2 Extend the shared CSS tokens and workspace styles so the settings page, sidebar, and primary surfaces remain legible in every supported theme.

## 3. Verification and localization

- [x] 3.1 Add or update UI coverage for settings navigation plus language and theme persistence across reloads and page changes.
- [x] 3.2 Update English and Spanish resource strings for the settings page, language selector, and theme selector.
